base = int(input("Enter the base of the triangle: ")) # Asks user to enter the base of the triangle then converts into integer
height = int(input("Enter the height of the triangle: ")) # Asks user to enter height of the triangle then converts into integer
print("The area of the triangle =", (0.5 * height * base)) # Calculates the area of the triangle then prints result

# input_values = input("Enter the base and height of the triangle: ").split()
# base = float(input_values[0])
# height = float(input_values[1])
# print("The area of the triangle =", (0.5 * height * base))